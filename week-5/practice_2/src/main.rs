 fn main() {
        let empty_string = String::new();
 
    print!("Length of empty sring is {}",empty_string.len());

    let content_string = String::from("ComputerScience");
    println!("\nLength of content_string is {}",content_string.len());
 }
